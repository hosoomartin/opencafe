spring:
  devtools:
    restart:
      enabled: false
  datasource:
    url: jdbc:h2:mem:opencafe
    driverClassName: org.h2.Driver
    username: opencafe
    password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true 